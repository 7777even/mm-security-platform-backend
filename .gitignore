# 构建产物
target/
.flattened-pom.xml

# IDE
.idea/
.vscode/
*.iml
*.ipr
*.iws

# 系统与临时输出
.DS_Store
*.class
*.log
logs/
nohup.out
*.out
*.tmp

# 本地覆盖配置（禁止入库，密钥一律走环境变量）
application-local.yml
application-*.local.yml
.env
.env.local

# Maven Wrapper 下载的 jar（本项目只提交 .mvn/wrapper/maven-wrapper.properties）
.mvn/wrapper/maven-wrapper.jar

# 本机 Maven 构建配置（含硬编码 localRepository 路径，机器相关，禁止入库）
ci-settings.xml

# 本地 H2 文件库（dev profile 持久化用，禁止入库；库文件由 Flyway 自动建表灌种）
data/mm_security_dev.mv.db
data/mm_security_dev.trace.db
data/mm_security_dev.lock.db
