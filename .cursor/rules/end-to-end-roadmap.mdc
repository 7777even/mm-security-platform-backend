---
description: 端到端路线图纪律——任何 L3/L4 改动开工前，必须先读跨库路线图→进度台账→当前进行中 Change，且不得为后续阶段预先搭建占位实现。改动业务/接口/数据结构/部署前必读。
alwaysApply: true
---

# 端到端路线图纪律（End-to-End Roadmap Discipline）

本系统是**跨库、分阶段**交付（后端 `docs/architecture/roadmap.md` 定义 7 个阶段 + 跨库依赖）。AI 在动手前必须建立「我在路线图的哪一步」的全局认知，避免两种典型失误：**为后续阶段提前铺摊子**（产生无人使用的占位代码/表/接口），或**无视阶段依赖**直接改后续阶段才该动的资产。

## 1. L3/L4 开工前三读（顺序不可省）

1. **读路线图**：`backend-scaffold/docs/architecture/roadmap.md`——确认当前所处阶段、本阶段交付物、与前后阶段的依赖边界。报告《AI编程规范与过程资产对标分析.md》的落地清单（P0/P1/P2）也在此图内，按优先级推进。
2. **读进度台账**：本仓库 `openspec/changes/` 下各 Change 的 `tasks.md` 状态（已 `approved` / 进行中 / 已 `archive`）。确认本次改动归属哪个 Change、是否已有进行中的任务。
3. **读当前 Change**：若本次改动属于某个进行中 Change，先读其 `proposal.md` / `design.md` / `tasks.md`，严格按已确认范围实现，**不擅自扩大**。

## 2. 禁止「为后续阶段建占位」（硬规则）

- ❌ 不得为路线图里**尚未到达**的阶段预先创建：空 Controller / 空 Service / 空 Mapper / 空表（Flyway V 文件）/ 空 `if` 分支 / `TODO` 接口。
- ❌ 不得把「将来可能要」的字段、枚举值、配置项提前塞进当前实体或契约（污染当前 Change 的契约真源）。
- ✅ 若发现当前实现确实受后续阶段约束（如字段需预留），**先在路线图/Change 中记录该约束**，再在当前阶段以最小必要形式落地，并标注「受 X 阶段约束」。
- ✅ 后续阶段真正到来时，由那时的 Change 显式扩展——那时才有完整上下文，避免占位即技术债。

## 3. 与既有规则的衔接

- 本规则是 `backend-scaffold-core.mdc`「先判级再动手」的**阶段维度补充**：分级管「改动多重」，本规则管「改动在路线图的哪一步」。
- 跨库契约真源仍在前端 `frontend-scaffold/docs/api/*.openapi.json`，本规则不替代 `api-contract.mdc` 的四同步要求。
- 任何对路线图阶段边界的质疑，先读 `roadmap.md` 并在对话中说明，再决定是否跨阶段——禁止静默跨阶段实现。
