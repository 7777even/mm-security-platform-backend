# QA — <变更名称（一句话）>

- 日期:<YYYY-MM-DD>
- 效率等级:<L3 / L4>（对应改动等级）
- 范围:<受影响的包 / 类 / 表 / 端点，列举关键路径与受影响目标。>

## 验收口径

- <逐条列出验收标准，引用 AGENTS.md §5 DoD / §3 契约 / §6 约束条款。>
- <例：新增端点返回 B3 包络 `code=0` 才取 data；20 位 MDM 编码非法时返回 301；硬控路径 POST 返回 503。>

## 实际执行命令与结果

- <实际跑了什么命令，通过多少、失败多少。禁止用无关全量命令冒充目标验证。>
- <例：`./mvnw test` 通过 N 例 0 failure；`node scripts/check-api-contract.mjs` 无差异；`smoke-test.ps1` 5/5 通过。>

## 未运行项

- <明确哪些没跑、为什么（Docker 不可用导致 IT 未执行 / 非本变更链路 / 环境限制）。禁止把未执行写成通过。>
- <例：带 DB 的 `*IT` 因未引入 Testcontainers 未执行，以 `./mvnw test` 零 DB 结果 + 冒烟覆盖。>

## 证据附件

- <接口类改动附 curl 请求/响应片段或 `smoke-test.ps1` 终端输出快照；契约类改动附前端 `docs/api/*.openapi.json` 的同步记录。随本报告置于同目录（引用文件名，不内嵌二进制），须能佐证"验收口径"逐条达成。>

## 结论

- <一句话结论：达成 / 未达成，遗留项与建议（如另立变更修复与本改动无关的既有缺陷）。>
