# 设计文档：<变更名称>

> 适用：L3 / L4。记录架构决策、取舍、风险与依赖。L1 / L2 不建此文件。
> 与本变更 `proposal.md` / `tasks.md` / `spec-delta.md` 四者闭环，人工确认后才动手（AGENTS.md §1.1 / §7.1）。

## 目标与约束

- <设计目标：要解决的可验证问题，能被 tasks.md 验收标准逐条印证。>
- <硬约束：来自 AGENTS.md §3 API 契约、§6 既有约束（分层 / 安全 / 数据）的不可妥协项。>

## 架构与方案

- <分层落点：Controller / Service / Mapper / Entity / DTO 各自改什么；是否新增包或模块。>
- <接口设计：路径、方法、入参出参、错误码、鉴权与 `x-permission`；与前端契约文件的对应关系。>
- <数据模型：表 / 字段 / 索引 / 约束变化；逻辑删除与审计字段；迁移与回退路径。>
- <安全设计：是否触及过滤器链、鉴权白名单、签名、硬控名单；密钥注入方式。>
- <复用优先：是否可复用既有 `common/` `security/` 能力，避免重造。>

## 决策记录（ADR）

- 决策 1：<选型 X 而非 Y> — 理由：<...> — 反对项：<...>
- 决策 2：<...>

## 风险与缓解

| 风险                             | 可能影响 | 缓解措施                                        |
| -------------------------------- | -------- | ----------------------------------------------- |
| <如：改表影响存量数据>           | <...>    | <...，增量 SQL + 回退脚本，先备后改>            |
| <如：契约变更前端未同步>         | <...>    | <...，同交付改前端 openapi.json 并重生成类型>   |
| <如：横切安全层回归>             | <...>    | <...，先走 L4 人工确认 + 全量 `./mvnw test`>    |

## 依赖

- <上游依赖：接口契约、前端库 `docs/api`、其他 Change（openspec/changes/<name>）。>
- <下游影响：依赖本接口的调用方、前端页面、生成类型。>
- <待确认项：标记 `#TODO-确认` 的未决点（如是否引入 Flyway、是否启用 Testcontainers），不得默认为已决。>
